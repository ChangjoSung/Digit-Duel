# #93/#104 전체 시점 감사 — Saturn 1차 REVISE

> 2026-09-08 · Mercury가 READ_ONLY Saturn_1의 inline 보고 msg_ebd1934d8bb5를 취합. 제품 관찰과 감사 도구의 판정을 구분한다. 본문 첫 표기의 인코딩 손상은 아래 명시된 REVISE 판정으로 해석했다.

[???] Saturn_1 independent #93/#104 orientation QA ? REVISE (audit false-green); frozen product observations PASS within the scope below; original CJ session OPEN pending play QA.
Role=Saturn; mode=QA; area=QA; mutation=none; instance_index=1; provider=codex. Task task_4ebe89dfdc7b / dispatch ctx_7aebcbbb4232. filesModified=[]; no report artifact created, PD should persist this inline report.

Confirmed blocker in demo/test/smoke_orientation_audit.js:188,233-244,454-455: step() calls agree(), pushes a mismatch into fails, and returns false without incrementing fail; teleport steps ignore that return, and both printing of fails and process exit depend solely on fail. I verified this with an additional narrowly targeted in-memory product mutant prompted by this new concern: immediately before cell.appendChild(chip), when NET.mode && NET.me===1 && S.teleport && S.teleport.stage===2, change chip.className from p+owner to p+(1-owner). This deliberately displays the wrong owners on the P2 board only during second-stage teleport selection, then the normal next render restores the board. The unchanged author assertions reported pass5194/fail0 and exited0 even though runtime inspection found 28 owner-class comparison failures (one for each scripted teleport); first failure: G1post P1 tele pick1 P2-screen<-P1.S, owner class at logical(13,1), pc p1 hiddenId. Thus this is a demonstrated false-green, not an unsupported anchor or a hypothetical gap. Tooling owner must make every observed mismatch affect exit1 and make callers/summary account for it, then rerun; Saturn edited no files.

Additional evidence/accounting limits: AUDIT.done/skipped are incremented at lines139-145 but never emitted or asserted by the shipped headless script, despite the report claiming a counter prevents vacuous success. battleLoop has a bound and can return without an explicit battle-completed assertion; subsequent board checks can skip while battle/overlay is open. These should be hardened together with the failure accounting. Browser comparisons are stronger for physical position, but CANON is a selected snapshot, not every field of S (for example it omits cds, memos, traces, inventory, reserve and selection). Headless canonical coverage is wider but also not literally every S field. Both art checks only reject a wrong path if an img/src exists, so neither establishes image-load success by itself. Pixel elementFromPoint is asserted 91/91 at initial boards; later afterEach only gates a.bad, so do not claim hit-testing was asserted 91/91 at every step. State/pixel full-board checks occur after moves and turn changes; selection checks inspect highlight geometry and state settling, not a full pixel audit immediately after every selection. Explicit memo-marker, trace, tutorial-hint and forced-target-highlight position assertions are absent from these two new tools; noise memo opening/closing is not full marker-placement coverage.

Frozen source provenance: git 6baa0b5f618b147029fdf5078a78b564753b0133, demo/index.html git blob a83260aeca33d0f63c6f87f53279ea42adb1436f, SHA256 d77be36dcd5d9528ea0cbd2c96286e6c865f9a59b1fcdb0b67e78c588a044fef. No active #106 product was tested and no #106 QA is claimed. Both tools default to git-show content in memory. The Chrome tool navigated two same-origin blank URLs then injected this exact frozen document with Page.setDocumentContent; actual WS matchmaking and hello handshake used the real loopback relay. Browser runtime Node v24.16.0, Chrome 152.0.7977.82, ws 8.21.3, viewport1280x1000.

Commands and independent results:
1) node demo/test/smoke_orientation_audit.js: exit0, pass5194/fail0. This is 5194 aggregate author ok assertions, not 5194 games or pixel samples. Fuzz for this invocation: battles58, forced50, pushes1, tele0, successful flee6, captures1, searches0, BT6/6.
2) node demo/test/orientation_audit_cdp.js --read-only: exit0, pass19/fail0. Actual two Chrome tabs; A=P1/B=P2, initial actor P2. Both used six distinct roster cards and 14 manual tray-to-cell placements, actual matchmaking, and local setup flip0. Setup positions preserved14/14 for each player. Initial P1 identity91/91; P2 reflect91, rotate13, identity7; elementFromPoint91/91 each.
3) node demo/test/smoke_orientation_audit.js --mutate flipOnCross --seeds 1: exit1, first mismatch at P2 crossing row8, pre and post paths detected. Compact repeat for untruncated count: pass3027/fail59.
4) node demo/test/smoke_orientation_audit.js --mutate rotate180 --seeds 1: exit1, first mismatch DOM#0 screen(1,1) contains logical(13,7), expected(13,1). Compact repeat: pass229/fail120; authored125 is not an invariant because initial actor/game RNG varies.
5) node demo/test/smoke_orientation_audit.js --mutate no104 --seeds 1: exit1, pass5184/fail10. Both actor directions diverged at hidden collision: P1 actor X=(10,4),Y=(10,3); P2 actor X=(4,4),Y=(4,3), plus battle/queue mismatch. All three required mutations matched anchors and reached real assertion failure exit1, none exited2.
6) Independent frozen-harness cross-check: loaded git-show 6baa0b5:demo/test/harness.js directly into Node Module cache in memory, then compiled the author audit with only an observation before its existing process.exit. Exit0, pass5194/fail0, fails=[]; full-screen audit invocations done15152/skipped748. This verifies the unmutated cross-check had no hidden recorded failures and no vacuous all-skipped result; those counts belong to this invocation only. Its fuzz battles57/forced49/pushes1/flee4/captures3/searches0/tele0/BT6/6.
7) Additional second-stage-teleport owner mutant described above: exit0 despite recordedFailures28, pass5194/fail0, done15296/skipped890. This is a failed detection control and the reason for REVISE. Runtime compilation and observation were memory-only; no test or application file was added or modified.

Coverage independently inspected and exercised:
- Headless fourteen scripted games = all7 columns x pre65/post65 routes, both players. Natural product roster toggle/manual placement/netPrepare/setupDone/matched/hello/hello2 paths, simulated WebSocket relay and DOM/timers rather than real browser/real server. Load-role order alternates, so flip is checked against NET.me rather than load order.
- Each pre game includes home exit, forward3, lateral, backward, forward, return lateral, then complete one-step crossing of midline7 and both forests to opponent row2 for P1 / row12 for P2 before65. Post games reach those rows after natural skip/end turn progression into BT. Both test forward/back/lateral two-step cases outside expedition zones; pre games also continue with a second minion after65. No direct turnCount injection.
- Real Chrome: one complete pre65 crossing game, P1 col2 and P2 col6, reached(2,2)/(12,6) at displayed turn28, bt=false; 52 cell payloads for pre path (28 preliminary +24 full crossing). Natural skip/end buttons advanced to displayed turn65. Second minions started(11,1)/(3,4), exercised BT forward2/back2/forward2 and crossed to(5,1)/(9,4), adding28 cell payloads. Total80 actual outgoing cell actions =40 selections+40 destinations, 40 per player, observed by pass-through WebSocket.send instrumentation; mismatch0. Highlight pixel positions, actor/nonactor display, destination state on both clients and subsequent 91-cell geometry were checked. No column or actor-turn flip observed. Chrome post path stops at opponent forest5/9; full post65 row2/12 routes are headless, not this Chrome run.
- Hidden-forest two-step collision: both actors naturally staged in headless, actor-independent visibility oracle, stop at intermediate col4, temporary reveal, forced battle and subsequent replay checks. Required no104 control reproduces the characteristic divergence; this confirms the known #104 path, not the original whole CJ session.
- Teleport swap:28 scripted natural headless swaps (both players across14 games), eligibility, stage1 highlights, swap canonical destination checks and remote display; the additional stage2 mutant exposes ignored stage2 oracle errors. No real-Chrome teleport/collision/push/flee replay in this browser tool.
- Push/flee: default headless fuzz actually produced1 push and6 successful flees, with paired canonical/display checks after actions. Counts are stochastic and not enforced as required minima by the shipped tool; not a guaranteed direction-by-direction push/flee matrix. Fuzz had1 capture and no search; telemetry is actual success metrics, not button attempts.
- End: natural surrender in Chrome and headless, both over and full reveal with fixed P2 reflection. Headless rematch is a fresh pair of loads with first load assigned P2, not an end-screen UI rematch on the same live server/tabs.
- Explicit fixture: four direct state placements cover all91 cells with28 pieces and compare both screens; clearly separate from natural gameplay. It is a transformation property fixture, not natural board traversal.
- Core independent oracle is not dataset-only or paired-state-only: headless row-major DOM position / Chrome sorted actual bounding-box centers is compared to the specification's independent row-reflection transform and the OTHER client's canonical pieces/visibility. Normal movement screen-vector formulas have algebraic elements, but chip placement, pixel clicks, emitted payload and opponent state add non-tautological checks. Required rotate180 and cross mutants demonstrate useful detection, subject to the confirmed failure-accounting hole.

Code/contract finding: frozen renderBoard uses a single row reflection r=14-i and unchanged c; input closure sends logical(r,c); applyNetSetup mirrors P2 placement rows only; boardFlipped depends on NET.mode/NET.me, not turn/position; the fixed onCellCore tests visibleTo(S.current,p). This matches spec section5. The 180-degree theory was investigated as a cause hypothesis and is unsupported in the tested frozen source. This is not a requested redesign or a new approval gate. The original same-server/same-version CJ session has not been replayed, and no-reproduction does not establish it solved; #93/#104 original whole symptom remains OPEN pending CJ play QA.

Dependency inspection/impact: read full CLAUDE.md and orientation report plus spec5; inspected both tools, harness, server/server.js, server/security.js and dependency surface. Current harness differs from frozen by11 added lines: #106 symbol/getter exposure and conditional BAL.fx.autoEnd=false. Frozen product has no BAL.fx, so that branch does not alter this source; the complete frozen-harness memory rerun confirms5194/0 independently. No full106 tests run. Approved demo/assets/minions images were served read-only; no art/ or orca-hook-latency-report.md access.

SHA256 before=after for every entry below:
demo/test/smoke_orientation_audit.js 9f29bbb53147b2cd8dd8ac34139b7ba808b320ae68dfb0b29306e1c1d807dc83
demo/test/orientation_audit_cdp.js 2bbea1fc86c886836ada96f9ca6ad2d292f09ac67c5ee48aaf63cf3a30499982
docs/qa/issue104-orientation-audit.md eab81d778c07139b0ce511fa124d8f02dc99828f1898d02b1ec3e5dc57915575
demo/test/harness.js 8a3596fdf73acdcec4c071e435c5b64ffaee371211b825e9a43df0e2ff0d336c
frozen harness blob0fb14f5973ad39426464eb546bca210ab65475fc / SHA256 cf1dec192b1b05a0a5ec125fdde9d99810e36388831d6464354ce9b4ac321a60
server/server.js a774b86a122710b8ac84eda3a8d305055642e190eb28123548954c44a794422e
server/security.js 0ea54ceb244d89a546d17674aff31551b7c302cd3e328f77e73c1bb6f0eb8c48
server/package-lock.json c799c5bf0e7798b5cb9b01ec1c9f8cbf9ed6911b1279465380281d0824060ce1
server/node_modules/ws/package.json a56a3fd55945a3ce177e3ca165dafae6f7eb03b7aefd58c092ac723d5741a6fb

Authored evidence provenance (not screenshots of my run): viewed all four existing PNGs with view_image. Author JSON says sourcegit:6baa0b5, product SHA above, timestamp2026-09-08T06:47:34.760Z, readOnly=false,19/0,80clicks,stepBad=[], viewport1280x1000. Screenshot2 shows displayed turn27 with noBT, P2 own traverser at screen row2 col6 and opponent at screen row12 col2; screenshot3/4 display turn79BT, second own traversers at P2 screen row5 col4 / P1 screen row5 col1. Columns visually retained and own home at bottom. Toasts partially cover top rows, so these PNGs alone cannot prove all91 chip positions; numerical geometry/payload tests supply the broader evidence. Current author PNG/JSON hashes match the report and remained unchanged; my read-only run produced zero PNG/JSON files.
Within docs/qa/issue104-orientation-audit/:
http_desktop-1280_1-p2-start.png 22635decaa4e66c437c87f5589786c1acf996cc198b85ee09e38408f591126cc
http_desktop-1280_2-p2-pre65-after-crossing.png e04ad652c5dffca34d748f725731a4c727a5f31698cd34867bfc0e3c76ff291d
http_desktop-1280_3-p2-after-crossing.png 0cc8e8b0753beff000f8483b7a544dc85fac825753a5e6591496f6328eb3a9e0
http_desktop-1280_4-p1-after-crossing.png bd8f54a9e97017bff87cf3a86fa20916a55f03e7a59cf6a1bb7613ceb426294a
orientation_audit_cdp_report.json 76209266475929bda480faa846a4f2034ce72110e254ae7b74fb28e1c06fc8c9
cdp_read_only.log 78d34673d9112190466075982524fdaff38b42c0180b13be76d03fc9a6b27cd5
headless_verbose.log 4c7cf9c4384491651dd8d3adfd69474c51081cf1ffaccb4c8f55715ca7ab7bee
mutants.log 018f474a8db2ac531b1ba7ed6339a8cbc508e9a2deeb0defcaa86f1f63caa8ba
README.txt 0713fbbd900c2d78b4d87a009e5c7bca71f4f7017c254483195e3c891a3eb809

Runtime scope and cleanup: tool-owned server PID35028 bound127.0.0.1:56744 using PORT0; Chrome PID84844, exact profile C:/Users/pc_77/AppData/Local/Temp/orient-cdp-aqdozA. DD_LAN/DD_BIND were unset. --read-only gates output directory creation, screenshot writing and report writing; only approved temporary Chrome runtime profile was created. Tool printed CLEANUP and exited0. Independent postcheck: both exact PIDs absent, exact profile absent, matching-profile Chrome process count0, listener56744 count0. No broad process kills or directory cleanup and no touch to user8080. FilesModified=[]; Git/GitHub/Notion writes0; no application/report/screenshot/tempHTML/tempJS/JSON writes by Saturn.

Disposition: QA work is complete with REVISE for the demonstrated audit false-green and evidence-accounting claims; observed unmutated frozen-product paths passed. Route tooling corrections to Mars and rerun independent QA, while retaining the separate original CJ play-QA OPEN status. No planning question or new approval request is needed.

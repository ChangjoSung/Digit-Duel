# Issue #106 실제 브라우저 보완 감사 — Mars_2 저자 보고 (Saturn QA 전)

- 작성: 2026-09-08 Mars_2 (CLIENT_TOOLING · IMPLEMENT · TOOLING · mutation=code · instance 2 · Claude Code) · Task `task_bc20276715f0` · Dispatch `ctx_7e1fba3a5a28`
- 소유 산출물: `demo/test/issue106_browser_audit.js` · 이 문서 · `docs/qa/issue106-browser/`(PNG 18장·JSON). 제품·기존 테스트·README·자산·`art/` 무수정. Git/GitHub/Notion 쓰기 없음.
- 관계: Mars_1 `demo/test/issue106_cdp.js`(자연 배치·턴 배너·자동 종료·하수인 전투·회복 +5/+5·폭탄→하수인·함정 공개·65턴 배너(빠른 600ms)·PVE 대기)와 헤드리스 매트릭스(`smoke_turnflow.js` 198·`smoke_turnflow_timers.js` 36)는 고치지 않았다. PD 축소 범위(msg_9ba29e7dd7aa · msg_c2d55433410b · msg_15358fcdf60b · msg_c63224299055)의 **기본 시간(BAL.fx 기본값) 필수 보완**만 검증한다.
- 판정 표기: **저자 PASS** = 도구 단언 통과(Saturn 독립 QA 아님) / **관찰(WARN)** = 판정·종료 코드 제외 / **미측정** = 이 도구가 다루지 않음(소유자 표기). 확정/추론 구분은 각 행에 표기.

## 1. 한 줄 결론 [확정]

최종 소스 `demo/index.html` **sha256 `a4932cb80a1407fc728ace6ee918315880840c76010de7b21361a7f618e97280`**(git blob `f580e4f9`, 282,568 bytes — Mars_1 msg_52b446d6768e 공지와 일치)에 대해, 동결된 도구 **sha256 `756a2236f1bf5ab5f59025fccf041695bdbb15bea5816efd4fd1f294394651c0`** 로 hotseat·pve·online 전 섹션을 **기본 연출 시간**으로 1회 완주: **64 pass / 0 fail / 2 WARN(판정 제외) / 66 단언 · 284초 · 스크린샷 18장(누락 0) · 픽스처 4건**. 필수 항목 — 가방 보너스 행동, 방어막→HP **실제 바 너비 시간 순서**, HP 비율 판정·동률, PVE AI 대기, 핫시트 기기 넘김, 390px 전투 메뉴, **65턴 배너 2초** — 전부 저자 PASS. 두 WARN 은 #106 범위 밖의 기존 390px 페이지 폭(5절).

## 2. 도구 · 실행 방법

```
node demo/test/issue106_browser_audit.js                     # 정상: docs/qa/issue106-browser/ 에 PNG·JSON (약 5분)
node demo/test/issue106_browser_audit.js --read-only          # Saturn 재검증: 산출물 0(디렉터리도 만들지 않음) · stdout 만
node demo/test/issue106_browser_audit.js --read-only --sections online   # B1s3·T65 필수 항목만 (약 3분)
node demo/test/issue106_browser_audit.js --ref <git-ref>      # git show <ref>:demo/index.html 을 소스로
```

| 항목 | 방법 [확정] |
|---|---|
| 소스 고정 | 시작 시 `demo/index.html` 을 **한 번 메모리에 읽고**(SOURCE 행에 sha256·git blob·bytes) 검증 전용 릴레이 서버(PORT=0 루프백 · 기존 8080 무접촉) 출처의 빈 URL(`/qa-issue106-blank`, 404)에 `Page.setDocumentContent` 로 주입. 병렬 편집 중 작업 트리가 바뀌어도 두 탭·PVE·핫시트 탭이 같은 소스를 쓴다(Mars_3 `orientation_audit_cdp.js` 와 같은 방법). 아트는 서버가 디스크에서 서빙(승인 자산 읽기 전용) |
| 시간 | **BAL.fx 기본값 그대로**(H0 단언으로 페이지 값 == 계약 3.4). 빠른 구간 없음. 시간은 페이지 안 MutationObserver 가 기록한 배너·메시지 전환 시각(ms). 허용 폭 −120 ~ +1300ms(워치독 1000 + 관측 지연) |
| 입력 | 메뉴·로스터 카드·트레이·칸·턴바·전투 4카테고리·하위 메뉴·모달 버튼 전부 실제 좌표 마우스 클릭(`Input.dispatchMouseEvent`). 규칙 함수 직접 호출은 클릭 증거로 세지 않는다 |
| 관측(제품 무수정) | WebSocket.send 래핑(송신 프레임) · DOM MutationObserver(배너·msgBox·바·잠금·메뉴) · 바 요소 인스턴스 `style.width` own accessor(쓰기 동기 순서, 재렌더마다 재설치 — Chrome 은 프로토타입 접근자가 아니라 인터셉터라 프로토타입 트랩은 무효) · **실제 바 너비**: 행동 탭 전면화 후 Node 측 20ms `getBoundingClientRect().width` 폴링 + 페이지 안 16ms/rAF 샘플 병합 · PVE `aiStep/aiBattleAction` 호출 시각·`fxLocked()` · BT 세그먼트 `rand()` 호출 계수 |
| 픽스처 | 자연 진행으로 만들기 어려운 위치·HP 조건만 **양 탭에 같은 값**을 넣고 SNAP 동일을 확인(FIXTURE 행). 그 뒤 접촉·전투·모달은 전부 실제 네트워크+UI 경로. 4절 표 |
| 견고성 | CDP 요청별 30초 타임아웃(메서드명 포함 오류) · 스크린샷은 `Target.activateTarget` 뒤 10초 한도, 실패 시 SHOT-SKIPPED 로 기록하고 단언과 분리(최종 실행 누락 0) |
| 리소스·정리 | 임시 Chrome 프로필 `os.tmpdir()/i106audit-*`(headless=new, 창 없음)·PORT=0 서버만 생성. `finally` 에서 **그 실행의 pid·프로필만** kill·삭제(CLEANUP 행). 접두어 전체 kill 없음. 실행 중 멈춘 진단 실행 1건은 로그의 RESOURCE 행 pid(70680/32760/99212)·프로필만 수동 정리 |
| 읽기 전용 증빙 | 정상 모드 첫 실행 전 `--read-only` 5회 뒤 `docs/qa/issue106-browser/` 부재 확인. read-only 는 `mkdirSync`·`writeFileSync` 경로를 타지 않는다 |

## 3. 최종 실행 결과 (a4932cb8 · 기본 시간) — 필수 항목

| 필수 항목 | 단언 | 결과 · 실측 |
|---|---|---|
| 가방 보너스 행동 | B1k·B1l | **저자 PASS** — 쿨링수 사용 → itemFx 2002ms 잠금(카테고리 클릭 무시) → 같은 행동자 루트 메뉴 복귀, round 1.0→1.0 · actor 불변 · item 프레임 1 · act 0 · 양 탭 S 동일 |
| 방어막 바 | B1r | **저자 PASS** — 수호 자세 → shield 22 = round(120×18%), 방어막 바 18.33%, 효과 연출 2017ms |
| 흡수 산술 | B1s | **저자 PASS** — "28 피해! (흡수 26) · 유효 2", shield 26→0, HP 67→65 |
| **방어막→HP 실제 시간 순서** | **B1s3** | **저자 PASS** — 실제 바 너비 91샘플: 방어막 바 감소 시작 +1631ms(피해 그룹 시작 기준 창 안), **그 시점 HP 바 너비 100.5px = 옛 값 유지**, HP 바 감소 시작 +2298ms(방어막보다 667ms 뒤 ≈ barStep 600), 둘 다 +2718ms 완료 → damageFx 2s 안 |
| 쓰기 순서(보조) | B1s2 | 저자 PASS — `shfill-D=0%` 쓰기 → `hpfill-D=54.17%` 쓰기 |
| 싸우기 타이밍 | B1m~B1q | 저자 PASS — 기술 2003ms → 피해 2016ms, 피해 행동 전부 ≥2s, 클릭 직후 잠금, act 프레임 1, 잠금 중 클릭 송신 0 |
| KO·결과 배너 | B1u·B1v | 저자 PASS — "쓰러졌다!" → P1 "전투에서 승리!" / P2 "전투에서 패배,,," 2513ms, 양 탭 동일 |
| **HP 비율 판정·동률** | B2f~B2h | **저자 PASS** — 12행동 실제 클릭, 12번째 직전 F-tie → 응급 치유(무피해) → "⚖️ 남은 HP 비율로 판별합니다!" 2017ms → "새끼 화룡 100% vs 동료 100%" → 동률 → **방어자 승**(ties 1·judged 1·공격자 제거·동료 생존) → P1 "패배,,," / P2 "승리!" |
| 동기 모달 재개 | B2a~B2e | 저자 PASS — 접촉 배너 잠금 중 "출전 공개" 모달(P1 소유·P2 대기) 클릭 무시·송신 0 → 배너 뒤 재개 → "전투 시작" modal 프레임 1 → 3·2·1·배틀 시작! → 라운드 배너. [확정] 방어 동료에 예비 하수인이 없으면 출전 선택은 자동 본체(제품 `vipChoice`) |
| **65턴 배너 2초** | **T65a~g** | **저자 PASS** — F-bt65 뒤 실제 생략/자동 종료 → 양 탭 "버닝타임입니다! 2칸씩 이동 가능합니다" **2013ms(P1) / 2014ms(P2)** → 턴 배너 2016ms(행동자 "나의 턴!" / 상대 "상대 턴!"), 각 1회·같은 순서, 행동자 endTurn 프레임만(상대 0), rand() 호출 0/0, S 동일, btEnterTurn 65 |
| PVE AI 대기 | P1~P8 | **저자 PASS** — 실제 버튼 시작, F-pve 뒤 실제 클릭 접촉, AI 는 `fxLocked()` 중 0회 행동, 라운드 배너 종료 후 654~656ms(1회 3328ms 이상치, 모두 ≥550) 뒤 행동, 전투 완주(내 5·AI 6), AI 턴 "상대 턴!" 2013ms 동안 aiStep 0 → 657ms 뒤 행동 → 내 턴 복귀. 2차 실행에서 AI 가 내 말과 전투를 열어 내가 방어자로 응답하는 경우도 처리 |
| 핫시트 기기 넘김 | H1~H7 | **저자 PASS** — 배치·플레이 시작·자동 종료(1102ms) 뒤 모두 "기기를 넘기세요" 모달이 배너보다 먼저, 확인 뒤 "나의 턴!" 2010ms·잠금·배너 중 클릭 무시 |
| **390px 전투 메뉴** | B1j | **저자 PASS** — 390px CSS 뷰포트에서 모달 상자 오른쪽 374px(화면 안), 내부 넘침 없음, 4카테고리·싸우기 버튼 화면 밖 0·잘림 0(스크린샷 10·11 — "도망가기"는 2줄 줄바꿈, 잘림 아님) |
| 4카테고리·하위 메뉴·가드 | B1d~B1i | 저자 PASS — 하위 메뉴 4개 열고 닫기 송신 0·S 무변경, 싸우기 4슬롯(기본 공격 없음), 포획 가드 "30% 미만이어야", 도망 가드 "50% 미만이어야", 상대 탭 "상대 아이템 비공개" |
| 진입 연출 | B1a~B1c | 저자 PASS — 접촉 → 상황 → 3(1007ms)·2·1·배틀 시작!(1010ms) → 라운드 배너 2016ms, 거울 문구 |
| 콘솔 오류 | H9·P8·O9 | 저자 PASS — 0 |

타이밍 요약(ms): 핫시트 턴 배너 2010 · PVE 결과 2512 · PVE 상대 턴 2013 · 카운트 1007/1010 · 라운드 2016 · 아이템 2002 · 기술 2003 · 피해 2016 · 결과 2513 · 판정 2017 · BT 2013/2014 · 이어지는 턴 2016. 전부 기대값 +20ms 이내 [확정].

## 4. 픽스처 (픽셀 클릭 증거로 세지 않음) [확정]
| 이름 | 내용 | 이후 실제 경로 |
|---|---|---|
| F-pve | 내 하수인 1개를 AI 하수인 2칸 아래(인접 아님)로 재배치, PVE 로컬 | 실제 클릭 1칸 이동 → 강제 전투 |
| F-B1 | P1 M-L2 (11,1)→(5,1), 양 탭 SNAP 동일 | 실제 클릭 (4,1) → 접촉 → 전투 전체 |
| F-B2 | P1 M-F1 (11,5)→(5,5) · M-F1·P2 동료 hp=maxHp=1000, 양 탭 | 실제 클릭 (4,5) → 동기 모달 → 12행동 |
| F-tie | 12번째 행동 직전 양측 hp=maxHp · P1 슬롯2 쿨 0, 양 탭 | 실제 클릭 응급 치유 → 판정 |
| F-bt65 | 양 탭 S.turnCount=63 · btReached false · rand 계수 래퍼 | 실제 생략 버튼/자동 종료 → 65턴 진입 |

## 5. 관찰(판정 제외) · 제한 · 미측정
- **390px 페이지 레이아웃(H8·B1j2, WARN 2건)** [확정]: 390px 뷰포트에서 문서 가로 폭 778(보드)~929px(전투 중), 턴바 버튼 2개 화면 밖. 원인은 `#app` flex + `#board` 고정 7×52px 그리드 + `#right min-width:300px` — dev HEAD 40415c2 와 동일한 기존 CSS로 #106 범위 밖 [기획 필요 / 별도 이슈 후보]. PD 가 증거 첨부 WARN 으로 수용(msg_c63224299055). 전투 모달·4카테고리·싸우기 하위 메뉴는 넘침·잘림 0.
- **사이드 패널 표시 지연** [관찰·추론]: 스크린샷 12(아이템 사용 뒤)에서 모달 뒤 사이드 패널의 "아이템 2/5·쿨링수" 표기가 남아 있다. `battleModal` 재렌더는 사이드 패널을 다시 그리지 않고 다음 보드 `render()` 에서 갱신되는 표시 계층 지연으로 보이며 규칙 상태(`S.inv`, 양 탭 동일)는 즉시 반영됐다(B1k). 판정 제외, 참고만.
- 포획·도망 **성공/실패 분기**는 공유 난수라 한 실행에 한 분기만 관측된다(PVE 진단 실행 중 AI 포획 성공 종료 발생). 사용자 LAN 2기기 주장 없음(루프백 2탭).
- 이 도구가 다루지 않음(소유자): 자연 걷기 접촉·턴 배너·자동 종료 프레임 통계·회복 +5/+5·폭탄→하수인·함정 공개·65턴 자연 진입(빠른 600ms) = Mars_1 `issue106_cdp.js` / 회복 매트릭스(동시 여러 말·만피 유지·이동 해제·미공개 비누설)·폭탄 상황 2/5/6·턴 시작 인접 비발동·복수 대상 선택·텔레포트·강제 queue·오래된 타이머·워치독·수신 큐 보류·HP 지연 쓰기 가드(K 블록) = Mars_1 헤드리스 `smoke_turnflow.js`·`smoke_turnflow_timers.js` / 최종 독립 판정 = Saturn.
- 단일 실행의 시간 실측이며 반복 통계는 아니다. 헤드리스 Chrome(창 없음)에서의 실측이므로 실제 기기 체감은 CJ 플레이 QA 몫.

## 6. 실행 이력 · 해시 · 벽시계 [확정]
| 실행 | 소스 sha256 / blob | 섹션 | 결과 | 벽시계 |
|---|---|---|---|---|
| read-only #1 | a20be021… / 12f5ae9e… | hotseat | 10/10 | 9s |
| read-only #2~#3 | a20be021 | pve | 3/4(도구: 전투 모달을 차단 오버레이로 오인) → 10/10 | 144s → 45s |
| read-only #4~#5 | a20be021 | online | 22/31(도구 단언 결함) → 37/38(실패 1 = 390px 관찰) | 102s → 182s |
| 정상 #1 | a20be021 | 전체 | PVE 루프 반복 상한(도구)으로 중단 → 시간 예산으로 수정 | 154s |
| 정상 #2 | a20be021 | 전체 | `Page.captureScreenshot` 무응답으로 정지 → 자기 pid/프로필만 정리 · CDP 30s 타임아웃 추가 | — |
| read-only #6 | a20be021 | online(+B1s2·B1s3·T65) | T65 7/7 PASS · B1s2/B1s3 FAIL = 도구 계측 결함(프로토타입 트랩 무효·백그라운드 탭 샘플러 억제) → 인스턴스 트랩·탭 전면화·Node 폴링으로 수정 | 188s |
| 정상 #3 | **a4932cb8** | 전체 | PVE 에서 AI 가 먼저 전투를 열어 방어자 응답 미처리(도구) → 수정 | 188s |
| 정상 #4 | a4932cb8 | 전체 | 스크린샷 30s 무응답(hotseat·pve 28 pass) → 캡처 비치명·10s 한도·전면화 | 125s |
| **정상 #5 (최종)** | **a4932cb8 / f580e4f9** | **전체** | **64 pass / 0 fail / 2 WARN · 스크린샷 18/18** | **284s** |

도구 동결 시점: 정상 #4 뒤 캡처 견고성 수정까지. 이후 수정 없음. 도구 sha256 `756a2236f1bf5ab5f59025fccf041695bdbb15bea5816efd4fd1f294394651c0`.
Saturn 통보: msg_9fc68844624d(ctx_db164ebf2352) · PD msg_e8cc4e643280.

## 7. 산출물
- `docs/qa/issue106-browser/issue106_browser_audit_report.json` — 단언 66·타이밍·픽스처·스크린샷 목록·소스/도구 해시·벽시계
- `docs/qa/issue106-browser/01~18-*.png` — 핫시트 넘김 모달/배너/390px 보드, PVE 전투 메뉴/AI 턴 뒤 보드, B1 4카테고리(행동자·상대)·싸우기·포획 가드·390px 루트/싸우기·아이템 뒤 같은 행동자·전투 뒤 보드, B2 출전 공개 모달(소유자·대기)·동률 뒤 보드, BT65 배너(P1·P2)

# #106 Saturn 1차 판정 — REVISE

> 2026-09-08 Mercury가 Saturn_2의 중간 보고·실패 재현을 취합했다. Task `task_ac25f36f826b`, Dispatch `ctx_db164ebf2352`. Saturn 파일 수정0. 대상 제품 SHA256 `a20be021eb0048fb53a40639e749eb0e5eccfdf18ebde3506a2af1d0d925d073`.

## 확인된 결함

1. **이전 게임 메시지 타이머가 새 게임 큐를 지움** — OLD skill 재생 중 타이머를 보존한 뒤 새 게임에서 NEW skill·NEW damage를 시작하고 OLD 타이머를 실행했다. 세대가 달라진 `playMsgs` 분기가 새 게임의 `MSGPLAYING`을 false로, `MSGQ`를 빈 배열로 바꿔 NEW skill이 남은 상태에서 입력 잠금을 풀었다. 구 세대 콜백은 새 상태를 정리하지 않고 종료해야 한다. 근거 `msg_d847280b3dba`.
2. **보호막보다 HP 표시가 먼저 갱신됨** — `applyFx`의 스타일 쓰기를 독립 계측하니 HP80% → SHIELD0% 순서였다. 계약5.5의 보호막 흡수 우선 표시와 작성자 설명이 일치하지 않았다. 산술적으로 두 값이 맞는 것과 화면 갱신 순서는 별도 검증해야 한다. 근거 `msg_d847280b3dba`.
3. **도망 성공 뒤 추가 접촉 배너가 즉시 사라짐** — 실제 제품 함수로 낮은 HP 공격자, 다음 아군의 forcedQueue, 도망 성공 조건을 구성했다. 결과 배너의 `onEnd`가 `fleeSwapPrompt → drainForcedQueue → contactBannerFx`를 거쳐 같은 게임 세대의 새 배너를 시작했으나, 옛 `fxNext.done`이 다시 큐를 진행해 새 배너를 숨겼다. 로그에는 result2500ms·contact2000ms가 있으나 실제 잠금은 해제되고 forcedTargets와 교체 모달이 남았다. 세대 검사 외에 같은 세대 재진입도 보호해야 한다. 근거 `msg_db013b1c24df`.

`smoke_turnflow` F4·B11a의 항상 참인 조건은 증거에서 제외했다. 수정본에서는 의미 있는 조건으로 바꾸거나 제거해야 한다.

## 통과한 범위와 해석

- 신규 규칙171·짧은 실제 타이머28·현재 제품에 대한 수정된 헤드리스 시점 감사8922가 통과했지만, 위 결함을 잡지 못했으므로 이 수치로 전체 PASS를 선언하지 않는다.
- 기본 CDP 실제 Chrome은50/0으로 완료했다. 일반 턴2038ms, PVE AI2688ms를 관찰했다. 버닝타임 구간은 FAST600ms이므로 기본2초의 실제 브라우저 증거로 쓰지 않는다.
- 기존 온라인 테스트157은 임시 변이 파일을 가상 파일시스템으로 처리하는 메모리 실행으로 통과했다. 실제 파일 쓰기는 없었다.
- 보완 도구의 MutationObserver가 한 번에 HP·보호막을 읽는 방식은 산술 및 최종 표시 확인이며 시간 순서 증거가 아니다. `msg_7b1a62c66488`의 이 한계는 Mars_2에 전달했다.
- Mars의 프로필 접두사 정리 사건은 Saturn의 `i106cdp-iYH7yN` 검사에 영향을 주지 않았고 정확한 PID·프로필·리스너 정리도 확인했다. 자세한 절차 기록은 [PD 사건 기록](issue106-pd-incident.md)을 따른다.

## 후속

Mars_1의 좁은 후속 Task `task_409978b5ce51` / Dispatch `ctx_a87b85dea1bd`에서 수정·재현 테스트를 수행한다. 수정본의 최종 해시와 독립 재검수 결과는 별도 최종 보고로 남긴다. 첫 판정은 이력으로 보존한다.

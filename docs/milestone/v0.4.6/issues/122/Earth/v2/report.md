# [피드백] #122 — 현재 HTML 기준 v2

Earth · 2026-09-10 · **제안 아트, CJ 최종 승인 아님.** 이번 수풀 보정은 `task_1326143e959e` / `ctx_9425bcbe8546`이며 Earth / IMPLEMENT / ART / assets·docs / instance null / Codex 계약에 따른다. 기존 v2를 갱신하고 이전 checkpoint `dd0d622`와 v1은 이력으로 보존한다.

| 정적 원고 | 표시 크기 | 수정 방향 |
|---|---|---|
| [board-v2.svg](board-v2.svg) | 420×910 | 7×13, 현행 52px 셀·48px 말·32px 아이콘·별도 11px 속성/HP 행. 풀은 실제 숲 4–5 / 9–10행에만 |
| [battle-v2.svg](battle-v2.svg) | 420×780 | 적 우상단·아군 좌하단, 반대편 HP판, 아래 메시지/싸우기·가방·포획·도망가기 |

보드의 일반 칸은 단색 네이비/진영색이고 숲 칸 내부 아래에만 각진 잎 군집을 둔다. 이번에는 수풀의 예시 `?` 말 2개와 해당 SVG 정의를 제거하여 풀만 남겼다. 은폐된 말의 위치를 암시하는 프레임·아이콘·HP 표시는 없다. 7×13 보드와 숲 28칸(4–5 / 9–10행), 그 밖의 말 배치·선택/이동 테두리·레이아웃은 유지한다. 실제 저장 경기나 전체 말 수의 증거가 아닌 정적 부분 배치다.

이 정적 배치에서 수풀에 보이는 유일한 아군 불 말에 카드 배경 `fill-opacity=0.50`, 승인 아이콘 `opacity=0.70`을 적용했다. **CJ 명확화: 수풀에서 보이는 말은 내 말·상대 말 모두 같은 반투명 원칙을 적용한다.** 상대 말의 새 배치를 추가하지 않고 기존 배치를 보존했다. 카드 아래 풀이 비치며 말이 수풀 안에 있는 느낌을 주고, 팀 프레임과 공개 가능한 속성·HP는 불투명도 1을 유지한다. 일반 칸의 말에는 투명도를 적용하지 않는다. 전체 그룹을 흐리거나 불투명 카드 위 아이콘만 흐리는 방식이 아니며 전경 잎을 추가하지 않아 HP를 덮지 않는다. 이는 Earth의 시각 제작값이며 최종 CJ 승인값은 아니다.

**가시성 한계:** CJ의 `?`가 안 나온다는 뜻은 수풀에 있고 상대가 근처에 없을 때 안 보이는 기존 은폐 규칙이다. 이 원고의 완전 은폐 예시를 제거한 것이며 실제 게임의 인접/`tempReveal`로 위치가 보이되 정체는 미공개인 `?`를 무조건 없앤다는 뜻이 아니다. `visibleTo`·`known`·인접·`tempReveal`·메모와 공개 여부 판단·좌표 변환은 기존대로 보존하며 제품 반영은 Mars 범위다.

기존 말의 파랑/빨강 프레임·공개 정보 행을 유지한다. 왕/동료는 #124의 새로운 얼굴만 연결하고 팀별 전신 이미지를 중복 제작하지 않는다. 미공개 말은 정체를 드러내는 새 얼굴·HP를 표시하지 않는다. 현재 색·숲·레이아웃 함수는 `demo/index.html`에서 확인했고 기존 승인 `fire_std`/`water_std`의 icon/battle PNG를 읽기 참조했다.

전투는 제공된 휴대용 몬스터 전투 참조의 **배치만** 따른다. 게임명·메뉴·HP/보호막·기술 체계는 Digit Duel을 유지한다. 기존 전면 아트를 그대로 배치했으며 뒤집기나 실제 후면 아트로 주장하지 않는다. 싸우기 하위는 현행 4슬롯, 전부 불가 시 현행 수동 턴 종료이며 왕·동료 본체의 기본 공격은 유지한다. 가방의 패키지, 비공개 출전/재고/기술, 관전자·온라인 대기/핫시트 차례 정책은 생략한 화면에서도 보존 대상이다.

원고는 스크립트·HTML·외부 네트워크 참조 없는 SVG이며 승인 PNG를 상대 로컬 경로로 참조한다. 원고만 다른 위치로 옮기면 이미지 경로가 깨질 수 있으므로 저장소 위치를 보존한다. 이번에는 기존 Chrome CLI로 [board-v2.png](board-v2.png)를 420×910의 1:1 크기로 다시 렌더하고 전체 이미지와 수풀 160×106px 원본 크기 crop을 직접 확인했다. 은폐 예시 두 칸은 풀만 보이며, 공개 아군 수풀 카드 뒤의 풀·감쇠된 얼굴·속성/HP가 식별된다. crop은 메모리에서만 만들어 별도 파일을 남기지 않았다. 전투의 이전 420×780 렌더·128px 아트/HP판/하단 명령 확인 이력은 유지하되 이번 작업에서 전투 파일을 재검수하거나 변경하지 않았다. 반응형·제품 기능·게임 가시성·경기 결과 QA를 이 정적 그림으로 주장하지 않는다.

수정 파일은 이 폴더의 `board-v2.svg`, `board-v2.png`, `report.md` 세 개다. 승인 하수인 PNG·Downloads 원본·제품/도구/테스트 코드는 변경하지 않았으며 Git 쓰기도 없다.

이번 렌더 자원: `C:/Program Files/Google/Chrome/Application/chrome.exe`, 직접 시작한 PID `102788`, 새 프로필 `C:/Users/pc_77/AppData/Local/Temp/earth-122-ctx-9425bcbe8546-render`(생성 전 부재 확인). PID는 정상 종료/exit 0을 확인했고 후속 조회에서 해당 정확한 프로필을 참조하는 Chrome 프로세스는 0개였다. 새 프로필의 정확한 경로만 정리하려던 명령은 자동 승인 검토에서 `blocked by policy`로 실행 거부되어 프로필을 보존하며 납품에서 제외한다. 삭제 재시도·우회·프로세스 강제 종료는 하지 않았다. 과거 정리 차단 자원에는 접근하지 않았고, 과거 이력은 [통합 아트 보고](../../../124/Earth/art-report.md)에 보존한다.

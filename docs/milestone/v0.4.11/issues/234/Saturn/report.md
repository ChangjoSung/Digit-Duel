# #234 Saturn 백업 QA (Claude) — 1차 REVISE

2026-09-17. **#122 2차 전례 예외(CJ 지시)**: Codex 한도 초과로 Saturn(Codex) 교차 모델 QA 대신, 구현하지 않은 Claude Opus 5 high 인스턴스가 READ_ONLY로 검수했다. CJ 플레이 QA가 최종 게이트다. 이 예외를 정상 절차로 소급하지 않는다.
run `run_8d0a50871c21` / task `task_d0d4ad0d879f` / dispatch `ctx_7495f6bf1e63`. 대상 커밋 `60319fa`(PR #240). 파일 수정 0. Mercury가 메시지 원문을 기록했다.

## 판정: REVISE (결함 2)

### 확인 완료
1. 일반 30종의 이름·속성·아키타입, 6.3 스킬 120개의 이름·설명, 2차 골격(감전 예외), 전설 스킬 12개, 왕·동료 6.2 스킬과 5.3 (2) 값, 전설 스탯 21값이 GDD와 일치한다(node 메모리 안에서 대조, 저장 없음). V2_FX 실행기 수치도 코드로 대조했다.
2. ⭐N이면 앞 N칸이 열린다. 기본기는 100%·⌛0이다. ⌛ 증가·수면 포자는 기본기를 대상으로 삼지 않는다. 도망 실패 페널티는 스킬 밖 경로다. 4.6 연쇄 금지(chainLock·actSeq)를 확인했다.
3. Venus 권고가 반영됐다. Q7 천년목은 즉사·지속·반사 피해에 모두 적용되고, Q6은 스킬 사용 1회당 1번 판정하며, Q1 기술 교체는 비활성이다.
4. 기존 회귀 8개와 서버 검사 2개의 변경은 GDD 6.3·6.2·6.4·7.9에 근거하며 약화는 없다. 레거시 검사는 recruitSkillSwap=true로 켜서 보존했다.
5. 좌석 프레임: 상대의 미공개 칸은 개수·kind 없이 `{revealed:false}` 1개만 보내고, 미공개 말은 위치만 보낸다(element 없음). 락스텝 요약에 nextShockForce·sandStormR·onceUsed·sleepNext·nullifyNext가 들어 있다.
6. AI는 상대의 공개 전투 정보(HP·방어막·상태·속성)만 읽고 grade·skills에는 접근하지 않는다.

### 결함
1. **마녀 4차 '마녀의 장난'이 효과를 전혀 적용하지 않는다.** `demo/index.html:1156`에서 v2로 등록되고 `4414`에서 v2로 분기돼 `4481 witchApply`에 도달하지 않는다. 근거: GDD 6.4 "서로 다른 2개를 확률 판정 없이 적용". 재현: 마녀 스킬 세트로 act(A,3)을 하면 상대의 burn·weaken·shock이 0이고 자기 HP도 그대로다(피해만 들어감). 스위트에 이 효과를 보는 검사가 없다.
2. **사신의 낫이 환영 무도·번식 포자를 우회한다.** `demo/index.html:4388`의 reaper 분기가 `4414 execV2`보다 앞에 있어 `1376~1377` 처리를 건너뛴다. 근거: GDD 6.1·6.3 환영 무도 "다음 행동 1회 무효", 번식 포자 "기본기가 아닌 스킬을 쓸 때마다", Venus Q10 "다음 스킬 사용 1회". 재현: round=6, 사신의 HP 비율이 더 낮고 nullifyNext=true·breedR=2인 상태에서 act(A,3)을 하면 즉사가 성립해 전투가 끝나고, 번식 포자 피해도 없다.

수정 참고: 결함 1을 고치면 난수 소비가 바뀌므로 락스텝·서버 경계 검사를 다시 확인해야 한다.

## 실행 검사 (각 1회)
- `smoke_issue234.js` 290 pass / 0 fail (exit 0)
- `test-issue234-boundary.js` 47 passed / 0 failed (exit 0)

## 미검증
전체 회귀·CI 6개(PR #240에서 따로 전부 통과), 브라우저·온라인 실플레이, 모든 스킬 조합과 경계 전수(코드 대조는 대표 경로만), Notion 원본과 사본의 차이.

## 참고 위험 (결함 아님)
- 공개된 상대 뷰의 maxHp로 등급을 역산할 수 있다. #236에서 등급 성장이 들어오면 7.9를 다시 검토해야 한다.
- 균사 전환 '흡수 30%'의 의미는 [기획 필요]로 남아 있다.
- 공개된 상대 스킬의 남은 ⌛(쿨타임)이 노출된다.

---

# 델타 재검수 — PASS

2026-09-17. 같은 QA 인스턴스를 재사용했다(task `task_3d5a1d3e8061` / dispatch `ctx_1a4b71a06ce9`). 대상은 `60319fa` → `df10bcd`, READ_ONLY이며 파일 수정 0이다.

1. **결함 1 해소**: witchPrank가 80% 피해 뒤 WITCH_COMBOS(4C2 균등 6조합, 서로 다른 2개)를 force로 적용한다. 확률 판정은 없고, 회피되면 대상 효과가 없다. GDD 6.4는 조합 선택 방식을 정하지 않았다. 균등 추첨은 적용 확률 판정이 아니라 선택이므로 모순이 아니다(추론).
2. **결함 2 해소**: 추출한 v2PreUse가 종전 execV2의 순서(수면 → 무효 → 번식)와 호출 위치를 그대로 지킨다. 사신의 낫 분기도 즉사 계산 전에 이 함수를 호출한다.
3. **[기획 필요]**: 환영 무도와 번식 포자가 동시에 걸리면 무효가 먼저라 번식 피해가 없다. 무효된 사용도 ⌛를 소모하므로 '스킬을 쓸 때마다'에 해당한다고 볼 여지가 있다. 해석 문제이므로 결함이 아니다.
4. RV1a~c·RV2a·b는 수정 전 코드에서는 실패하는 단언이다. fixRand 0.55·0.45 변경은 조합 인덱스를 고르려는 입력 선택이며 기대값 약화가 아니다(RV1a·b 주석 '×0.98' 표기만 사소하게 부정확).

실행 검사(1회): `smoke_issue234.js` 296 pass / 0 fail, exit 0.
미검증: 서버 락스텝·경계(난수 소비 변화 — PR CI B가 확인), 브라우저 실플레이.

# #241 백업 QA (Claude) — PASS

2026-09-17. **#122 2차 전례 예외(CJ 지시)**: Codex 한도로 Saturn(Codex) 대신, 구현하지 않은 Claude Opus 5 high 인스턴스가 READ_ONLY로 검수했다. CJ 플레이 QA가 최종 게이트다. task `task_1144ac1e77ef` / dispatch `ctx_c746a0dc23a1`. 대상 `1e29f44` → `94127d0`. 파일 수정 0. Mercury가 메시지 원문을 기록했다.

## 판정: PASS (REVISE 결함 없음)
1. **번개 꼬리**
   - 추가 공격 단계가 pending에서 active로 바뀌고, ⌛ 사본 저장 뒤 ⌛를 0으로 만든다. 후보는 [0,1,2]다.
   - 추가 공격은 피해만 60%다.
   - 추가 공격 외 행동은 클라이언트 코어 5곳과 서버 `_authorize`에서 막는다.
   - 턴 끝에 `v2BonusEnd`로 ⌛를 복원한다.
   - 환영 무도로 무효가 되면 번개 꼬리 효과가 실행되지 않는다. 첫 타격에 상대가 쓰러지면 사망 판정이 먼저다.
   - 서버 요약에 추가 공격 단계가 들어 있다.
2. **해일 예고**
   - 피해 미리보기가 ①~⑩에서 멈춘다. 회피하면 표식이 없고 전투당 1회는 소모된다.
   - 판정 위치: 행동 종료 처리, 다음 행동 시작, 라운드 종료 뒤
   - 방어 효과 3종이 켜진 동안 보류한다. 해제·옮기기 대상이 아니다.
   - 양쪽 화면에 상태 아이콘과 X 선을 표시하고, 서버 요약·프레임에도 들어 있다.
3. **거울 수면**: 해제 순서표의 첫 상태이상을 옮기고, 영겁의 재 화상은 건너뛴다. 회피면 해제만 되고, 부여자는 거울 사용자로 바뀐다.
4. **회피율 감소**
   - −10/−15/−20%p, 2R, 확정 부여다.
   - 최종 회피율은 `effEvade` 한 식에서 0~0.40으로 자른다.
   - 해제·옮기기가 가능하고 💨 아이콘을 표시한다.
   - `spdDown` 잔재는 0건(주석 제외)이고, `effSpd`는 불변이다.
5. **단순화 12종**: 수치·문구가 승인값과 같다. 수면 포자는 현행 유지다. 클라이언트와 서버의 V2_TIMED 16키가 순서까지 일치한다.
6. **Q3**: ⌛0에서 +1한 슬롯만 부여 라운드 감소에서 뺀다. 라운드 종료·쿨링수에서 초기화되고, 사신의 낫 제외(P13)는 유지된다.
7. **기대값 변경**: smoke_issue234와 234 경계 검사의 변경은 모두 규칙 교체 단언이고 근거 주석이 있다. 약화는 없다.
8. **승인 밖 수치**: 없다. AI 가중치는 규칙 수치가 아니다.

## 실행 검사
- `smoke_issue234` 351/0
- `test-issue241-boundary` 74/0
- `smoke_issue241` exit 0
- **[절차 위반]** QA가 명령을 잘못 입력해 `smoke_issue241`을 2회 실행했다. 두 번째는 출력을 버려 통과 개수를 기록하지 못했고, 재실행은 하지 않았다. Mars 실행 83/0과 PR CI가 개수 근거다.

## 기획 필요
- **E5**: 천년목 버티기를 쓴 뒤, 또는 철벽 무효를 소진한 뒤에는 그 효과가 켜져 있어도 해일을 보류하지 않는다(`v2TideBlocked`). CJ 확인 권고.
- **집게 반격·빙벽 반사 공유 게이트**: 집게 반격(반사로 통일)이 `B.reflectSeq`를 빙벽 반사와 공유해 한 행동에 1회로 묶인다. 한 전투원이 두 스킬을 함께 갖지 않아 현재 종 구성에서는 영향이 없다.

## 참고 (결함 아님)
- 서버 요약의 `counterSeq`는 엔진에서 더 이상 쓰지 않는 키다(항상 null). 정리 후보다.
- 브라우저 화면·공개 방 실경기·Roblox 미러는 검증하지 않았다.

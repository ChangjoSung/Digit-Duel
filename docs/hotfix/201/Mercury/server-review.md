# 서버 구현 인수 및 보완 요청

2026-09-11 · task_40e9ad1845f4 / ctx_69d67cc37bdb · msg_1ec0734bce38. Jupiter 서버 구현 보고를 인수했다. 파일 범위는 server/server.js·security.js·package.json·README.md·test-static-load.js 및 Jupiter 보고서다.

실HTTP 기준판 동시2명 90요청 중29건429, 수정 후0건 실패. 고정 버스트288·초당96으로 조정했으며 기존 인증·경로 검사와 유한 제한을 유지한다. 서버 전체5스위트 exit0 보고를 받았다. 제품 서버는 Mars 브라우저 검증에 사용할 수 있다.

마지막 요청이 반드시429라는 신규 검사에는 운영체제 스케줄링으로 토큰이 회복되는 시간 경합이 있어 보완 요청했다. 정상200과 잘못된 허용을 혼동하지 않도록 전체 허용량 상한·거절 발생을 검증하며 제한 비활성화 변이는 계속 검출해야 한다. README의 검증 세부는 Jupiter 보고서로 모으고, Saturn에 npm 설치·파일 수정을 안내한 부분은 READ_ONLY 계약에 맞게 정정한다. 이 보완만을 위한 즉시 동일 범위 lease이며 최종 검수는 Saturn이 한다.

## 보완 인수

task_e91749c3626c / ctx_bb2bc20bba5d · msg_796b37beb6b1을 인수했다. 서버 런타임은 그대로이고 검사·문서만 수정했다. 실제 부하 검사는 전체 허용량 상한/429 발생을 유지하며 즉시 요청 경합은 시간을 고정한 버킷 검사로 옮겼다. HTTP 검사 요청에는10초 timeout·오류 단일 종료를 넣었다. 제한 비활성화 변이는1152건 전부 허용돼3개 부하 어서션이 실패하고, 항상허용 TokenBucket 변이는 결정적 소진 검사가 실패했다. 최종 신규 회귀 exit0(1152요청 중200:313·429:839), 무응답 검사는 축소400ms 시한에서410ms에 timeout 실패로 종료했다. READ_ONLY 안내와 별도 승인 필요 주장을 정정했다. 최종 독립 검수에 전달한다.
